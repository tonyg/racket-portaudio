#lang setup/infotab
(define collection 'multi)
(define deps '(
	       ;; these are what the package system tells me I need:
	       ))
(define build-deps '(
		     ;; these are what the package system tells me I need:
		     ))
